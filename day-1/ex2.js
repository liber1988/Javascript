let name = "Reuven"; //personal greeting
let age = 35;
console.log(`My name is, ${name} and my age is ${age} ! `);

const length = 5; //colculating rectangular area
const width = 3;
let area = length * width;
console.log(
  `The area of a rectangle with lengh of ${length} and width of ${width} is ${area}!`
);

let dayOfTheWeek = "Monday"; //to do list exersice
let task1 = "To clean the flat";
let task2 = "to cook for the weekend";
let task3 = "to play footbal";
console.log(`On ${dayOfTheWeek}, i will ${task1}, ${task2}, ${task3}`);

let numOfPets = 4;
console.log(`I want to have ${numOfPets} pets`);

console.log(
  `the area of the triangle ${
    length * width
  } with width ${width} and length ${length}`
);
